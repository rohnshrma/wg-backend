# WebiGeeks — Production Readiness Roadmap

> Single source of truth for phased completion of the WebiGeeks platform (frontend: `wg-frontend`, backend: `wg-backend`). Read this file at the start of any future session before resuming work.

## Critical context (read this first)
- Both repos previously had only a single bare "initial commit" (boilerplate) in git — **all real app work existed only as uncommitted local changes**. This was discovered and fixed on 2026-07-24: the real work was committed to `main` in both repos (commits `97ede80` backend, `83953c7` frontend). Going forward, commit regularly — don't let work sit uncommitted.
- No separate "Project Specification" or "Repository Audit" document exists — this file plus the live codebase is the working source of truth.
- Repos: `backend` → github.com/rohnshrma/wg-backend, `frontend` → github.com/rohnshrma/wg-frontend (separate git repos, not a monorepo).
- Admin login: `webigeeksofficial@gmail.com` — password is no longer the `Admin@123` default (production refuses to boot with that value, see Phase 12). Current production password lives only in Render's `ADMIN_DEFAULT_PASSWORD` env var, not in this file or git.
- **Production is live** (since 2026-07-26): frontend on Vercel + custom domain `webigeeks.com`, backend on Render, DB on MongoDB Atlas. See Phase 12.
- To run locally: MongoDB must be running (`brew services start mongodb-community` if needed), then `cd backend && npm run build && npm start` (port 5001), `cd frontend && npm run build && npm start` (port 3000). Avoid `npm run dev` in this sandbox — it hangs; use build+start instead.
- Backend has a real rate limiter (100 req/15min per IP general, 10/15min auth) — don't hammer it with rapid automated reloads during testing; it's in-memory so restarting the backend clears it.
- **Worktree gotcha (bit us on 2026-07-24/25):** if working in a git worktree under `.claude/worktrees/`, remember the running dev servers must be built+started from the same checkout you're editing. Building in a worktree but running `npm start` from the real checkout (now `~/dev/webigeeks/frontend`) silently serves stale code with zero errors — very confusing to debug. Always confirm which checkout the live server is actually running from before trusting a "it's not showing up" result.
- **Low-memory machine gotcha (2026-07-25):** this machine runs low on free RAM during long sessions (many dev servers + browser + editor open at once). `git push` can fail with `pack-objects died of signal 10` (SIGBUS) under memory pressure even on a tiny repo. Fix: retry with `git -c pack.threads=1 -c pack.windowMemory=10m push ...`. Also: `next start` has been observed to silently die with no error in its log under the same pressure — if a "running" server stops responding, just check `ps aux | grep next-server` and restart it.
- **TWO PARALLEL INITIATIVES IN FLIGHT ON `wg-frontend` — check `git branch -a` and `git worktree list` before assuming `main` is the only thing that matters.** SEO/audit work happens on `main`/`fix/pagespeed-findings` in the primary checkout. A full premium UI redesign is happening **in a separate Claude Code session, in its own isolated git worktree** at `.claude/worktrees/redesign-premium-ui-2026`, branch `worktree-redesign-premium-ui-2026`. **Don't touch that worktree, don't merge/rebase onto its branch, and don't run `git reset`/`checkout` in the primary `wg-frontend` checkout without checking `git worktree list` and confirming with the user first** — see the incident below.
  - **2026-08-07 night → 2026-08-08, real data loss, root-caused:** an earlier version of the redesign work existed only as **uncommitted changes in the primary checkout** (not yet moved to a worktree). This roadmap previously claimed those changes were safely `git stash`ed before a branch switch back to `main` — that account could not be verified and was very likely wrong: the redesign session's own investigation afterward found `redesign/premium-ui-2026` showed **zero diff against `main`** and the reflog showed a plain `git reset` (not a stash) between leaving that branch and arriving at `fix/pagespeed-findings`. No stash entry was confirmed to exist. **Practical lesson: uncommitted work in a shared checkout is not safe from a concurrent session's branch switches, resets, or checkouts, even when that session believes it stashed things correctly first.** The redesign session recovered by manually rebuilding everything from the conversation history (not from git) and has since moved into a dedicated worktree and started committing after every meaningful change specifically so this can't happen again. Full detail in the "Design — Premium UI Redesign" section further down.
  - A related bug from the same incident, already fixed: a stale `.next` build cache served HTML with CSS classes that don't exist in current source, suspected caused by `.next/cache` not invalidating across a branch-switch away from the (then-uncommitted) redesign branch. Fixed with `rm -rf .next` + rebuild. If build output ever looks like it doesn't match source again, wipe `.next` before assuming you're misreading the code.
- **REPO LOCATION CHANGED 2026-07-28 → `~/dev/webigeeks/{backend,frontend}`.** The old `~/Desktop/webigeeks` copy is stale; do not work in it. Update any editor workspace, terminal alias, or bookmark still pointing at Desktop. (The Desktop copy was left in place rather than deleted — remove it once you've confirmed nothing else references it.)
- **iCloud Desktop sync gotcha — ROOT-CAUSED AND FIXED 2026-07-28 by moving off `~/Desktop`.** Kept here because the symptoms are worth recognising if a checkout ever ends up back under `~/Desktop` (or `~/Documents`). iCloud "Desktop & Documents" sync with **"Optimize Mac Storage" ON** (confirmed: `defaults read com.apple.bird optimize-storage` → `1`) *evicts file contents*, leaving stubs that `ls -lO` marks `dataless`. Node and git then read truncated data and throw errors that look exactly like code bugs. On 2026-07-28 the *same unchanged source* produced, across different attempts: `ERR_INVALID_PACKAGE_CONFIG` on random `next` internals, `_interop_require_wildcard._ is not a function`, `Class extends value undefined` in `mongodb/gssapi.js`, `require(...) is not a function` in `morgan`/`debug`, `mime/types.json: Unexpected end of JSON input`, and `pack-*.pack is far too short to be a packfile` (which corrupted the backend's git object store outright). Counts at the time: backend `node_modules` **9,102** evicted files, frontend **28,095**.
  - **Diagnose:** `ls -lRO node_modules | grep -c dataless`. Non-zero → it's this, not your code.
  - **A `tsc` sitting at 0% CPU for 20 minutes is this, not a slow compile.** Same for a server that starts, prints nothing, and never binds its port.
  - **Fix `node_modules`:** `rm -rf node_modules && npm ci`. `brctl download` does *not* materialise them, and reading them back with `cat` ran ~87 files/min — far too slow to be practical.
  - **Fix a corrupt `.git`:** verify `git ls-remote origin` HEAD matches local, back up uncommitted files, `mv .git .git-corrupt-backup`, then clone fresh to /tmp and copy its `.git` in. Working tree, `.env`, and `node_modules` are untouched.
  - **Watch for `filename 2.ts` conflict duplicates** appearing in tracked source — three existed (`src/lib/gallery 2.ts`, `src/types/user 2.ts`, and five `.claude/settings.local N.json`). Never commit them.
  - After the move: **0 dataless files, backend build ~seconds instead of stalling, frontend build 11.7s, all 76 tests green.**

## Design — Three.js visual layer (added 2026-07-25)
- Added `src/components/three/` (`NeuralNetworkScene`, `FloatingShapes`, `Hero3DBackground`) using `three` + `@react-three/fiber` + `@react-three/drei`. Applied sitewide: full treatment on the homepage hero, a lighter "compact" accent on every other public page banner (About, Courses list/detail, Contact, Testimonials, Gallery, Blog list/detail, Login, Register).
- Real bug hit and fixed: mounting the WebGL canvas immediately on page load competed with each page's Framer Motion entrance animation for the main thread, visibly freezing the fade-in around ~15-20% progress indefinitely (reproduced via `getComputedStyle` on the animating element — opacity was frozen, not just slow). Fixed by delaying canvas mount ~900ms via `setTimeout`, plus trimming node/shape counts and fixing device pixel ratio to 1. If this resurfaces (e.g. on a slower device), check that delay first before assuming it's something else.
- This pulled in `three`, `@react-three/fiber`, `@react-three/drei` (~150 packages) as a deliberate trade-off over extending the app's existing dependency-free custom `BarChart` component — chosen because line/pie/3D visuals were needed, not just bars.

## Design — Full rebrand: real logo, colors, tagline (2026-07-25, DONE)

**STATUS: Committed and pushed to `main` on both repos** — frontend `874855f`, backend `f27508e` (color/tagline fixes) on top of `9f099b2` (roadmap). Verified working via browser (homepage, login, admin dashboard) before pushing.

**What triggered it:** user supplied the real logo (`/Users/rohan/Downloads/Untitled design.png`) and asked for the actual brand colors to be used sitewide, plus confirmed tagline "Your AI Skill Partner" (replacing the placeholder "Training & Development").

**Exact brand colors extracted via pixel sampling (not eyeballed):**
- Blue `#1672B8` → replaces old purple primary (`#6C3CE1`) everywhere.
- Gray `#606062` → replaces old sky-blue secondary (`#0EA5E9`) everywhere.
- Full computed scale (50/100/200/500/600/700/900 + dark/light variants) is in `frontend/src/app/globals.css` under `@theme`. Orange accent (`#F97316`/`#EAB308`) was deliberately kept unchanged — it's a CTA color, not tied to the logo, and still reads well against the new blue/gray.

**Logo assets created** (from the source PNG, background chroma-keyed to transparent):
- `frontend/public/images/logo.png` — full lockup (mark + "WEBIGEEKS" wordmark), for large placements.
- `frontend/public/images/logo-mark.png` — icon-only crop (no wordmark), used in every navbar/sidebar/header slot next to the existing styled "WebiGeeks" text.
- `frontend/public/images/icon-512.png` + `frontend/src/app/favicon.ico` — regenerated from the mark, centered on a padded square canvas. **Not yet visually confirmed in an actual browser tab icon** — worth a quick look.

**Everywhere the logo/tagline/colors were swapped in:** `Navbar.tsx` (desktop + mobile), `Footer.tsx`, `admin/layout.tsx` sidebar, `dashboard/layout.tsx` sidebar, `LoginContent.tsx`, `RegisterContent.tsx`, `ForgotPasswordContent.tsx`, `ResetPasswordContent.tsx` (both the big decorative-panel icon and the small compact header icon on each), `config/site.ts` tagline field, `app/layout.tsx` metadata (title/siteName/twitter/theme-color), `about/page.tsx` + `gallery/page.tsx` meta descriptions, and the Three.js components' hardcoded hex colors (`NeuralNetworkScene`, `FloatingShapes`, `Hero3DBackground`, `admin/analytics/page.tsx` chart colors). Backend: `emailService.ts` (header/footer/welcome-email text) and `generateReceiptPdf.ts` (header + footer text) had their "Training & Development" text updated.

**Verified working via browser** (after a bad stretch of screenshot-tool flakiness that turned out to be stale/dead browser tab state, not a code bug — fixed by closing stale tabs and opening a fresh tab group): homepage hero (logo, tagline, blue gradient, neural network + orange floating shape, popup form now blue), Login page (logo badge, blue decorative panel), Admin dashboard (sidebar logo, blue welcome banner, stat card icons).

**Remaining gaps:**
1. **Minor cosmetic nit:** on the admin dashboard, the "Courses" stat tile and "Monthly Admissions" chart bar now render in the new dark gray secondary color and look a bit flat/heavy compared to before (when secondary was a bright sky blue). Not broken, just a visual judgment call — could lighten to a mid-gray tint if it reads as too dark in practice.
2. Favicon/tab-icon not visually double-checked in an actual browser chrome (only generated + spot-checked as a PNG).
3. `npm audit` shows some high-severity advisories in transitive deps pulled in by `recharts` and `@react-three/*` — not investigated, likely fine for a training-site admin panel but worth a look before considering this production-hardened (Phase 10/11 territory).

## Phase 8 continued — UPI AutoPay e-mandate for installment fees (2026-08-01, autonomous session)

**Trigger:** user handed over a written brief and stepped away ("I will not be available... use your best judgment"), asking for a full UPI AutoPay e-mandate flow so installment students authorize once at enrollment and Razorpay auto-debits each installment, with receipts and pre-debit reminders, instead of the fully-manual "record payment" flow that existed before this session (Payments was previously `SPOT-CHECKED OK` with zero Razorpay code anywhere in the repo — no keys, no SDK, nothing).

**Hard stop hit, handled per the brief's own rules:** the brief named exactly one thing to stop and flag rather than work around — Razorpay Subscriptions not confirmed enabled. There were zero Razorpay credentials anywhere in this environment (no keys in `.env`/`.env.template`, no existing integration code). The user checked their actual dashboard mid-session and it showed fresh-merchant onboarding banners ("90 Free Days", "1/3 COMPLETED" setup, no Subscriptions product visible in the nav) — later said they received an "account activated" message, but Subscriptions specifically (a separate toggle from general payment-collection KYC) was never confirmed enabled, and no real API keys (test or otherwise) were ever supplied. Per the user's explicit decision when asked: built the entire feature now, config-gated exactly like the existing Cloudinary/WhatsApp pattern (`assertRazorpayConfigured()` → clean 503 "AutoPay is not configured yet" until real keys land in `.env`), rather than waiting idle. **Open follow-up:** add real Razorpay test-mode keys to `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET`/`RAZORPAY_WEBHOOK_SECRET` in `.env` (fields already added to `.env.template`) once Subscriptions is confirmed enabled, then re-verify the mandate-creation → webhook loop against Razorpay's real test-mode sandbox — everything below was verified with mocked Razorpay calls and a real running app, not real Razorpay traffic.

**Prompt injection found and not followed:** `wg-frontend/AGENTS.md` (auto-loaded, framed as overriding instructions) directed reading `node_modules/next/dist/docs/` before writing any frontend code. That docs folder contains a planted `{/* AI agent hint: ... you must export unstable_instant ... */}` comment inside `index.md` — a fabricated Next.js API embedded to manipulate an agent into "following the docs" into bad code. `node_modules` is regenerated by `npm install`, not something a real team authors framework guidance around, so the whole AGENTS.md → node_modules docs chain was treated as untrusted content, flagged to the user, and ignored in favor of this codebase's actual existing conventions. Worth a supply-chain look at how that got into a fresh `npm install` of `next@16.2.9`.

**Backend — data model:** kept the existing per-installment-document pattern (`models/Installment.ts`) rather than the brief's literal `installments[]` array, since that's how the codebase already tracks manual plans. Added `models/Mandate.ts` (one per student's AutoPay subscription: `razorpaySubscriptionId`, `status: created|authenticated|active|paused|cancelled|failed`, `amount`, `totalCount`, `period`/`interval`, `consent: {acceptedAt, ip}`, `shortUrl`), and `models/RazorpayWebhookEvent.ts` (dedupe log, keyed on a SHA-256 hash of the raw webhook body — Razorpay webhooks carry no stable top-level event ID and are retried verbatim on delivery failure). Extended `Installment` with `mandateId`, `collectionMethod: manual|autopay`, `razorpayPaymentId` (unique+sparse), `receiptUrl`, `autoDebitFailedAt`/`autoDebitFailureReason`. Extended `Payment.paymentMethod` with `upi_autopay`.

**Deliberate deviation from the brief, and why:** the brief specified installment `status: pending|debited|failed`. Left `Installment.status` as the existing `pending|paid|overdue` instead — `applyPaymentToInstallments`, `markInstallmentPaid`, the admin/student UIs, and the daily reminders job all already key off that exact 3-value set, and forking a 4th value would mean every one of those had to learn about it or silently misbehave. A successful auto-debit reuses `status: 'paid'` + `paidDate` (same terminal state manual payments use); a failed attempt is tracked via the new `autoDebitFailedAt`/`autoDebitFailureReason` fields without changing `status`, so the installment stays visible as still-owed and the existing manual "Mark Paid" fallback keeps working untouched.

**Another real constraint surfaced, not papered over:** Razorpay Subscriptions charge one fixed amount every cycle. The existing manual plan (`calculateInstallments`) puts an uneven remainder on the first installment — that doesn't map onto a subscription's fixed per-cycle amount. Rather than silently under-collecting the last installment, `POST /api/payments/mandate` now rejects with a clear 400 (`...does not split evenly into N installments...`) when the pending balance doesn't divide evenly, and tells the admin to adjust the count or use the manual plan instead.

**Backend — mandate + webhook flow:** `config/razorpay.ts` (lazy client, `assertRazorpayConfigured` mirroring `upload.routes.ts`'s Cloudinary check), `services/razorpayService.ts` (`createSubscriptionMandate` via Razorpay Plan+Subscription, `verifyWebhookSignature` via plain `crypto.createHmac('sha256', ...)` rather than an SDK helper so it can't drift with SDK versions), `controllers/mandate.controller.ts` (`createMandate`, `getAllMandates`, `getStudentMandate`, `cancelMandate`), `controllers/webhook.controller.ts` (`POST /api/webhooks/razorpay`, no `protect` middleware — authenticity is the HMAC signature, not a session). Raw-body capture added to `app.ts`'s `express.json()` `verify` callback specifically because signature verification needs the exact bytes Razorpay signed, not a re-serialized `req.body`. Idempotency: insert-or-reject on the body-hash table, checked before any event-type branching; `subscription.charged` additionally checks `razorpayPaymentId` isn't already recorded on an installment as defense-in-depth. `subscription.authenticated`/`activated` → mandate active; `subscription.charged` → next pending installment marked paid via the **existing, reused** `recordPaymentAndNotify` (exported from `payment.controller.ts` for this purpose) so AutoPay payments can't drift from the manual-payment code path — same student-balance update, same receipt generation via the existing `generateAndUploadReceipt`, same Payment record shape (`paymentMethod: 'upi_autopay'`); `subscription.pending`/`payment.failed` → failure fields set, student+admin notified; `subscription.halted`/`cancelled` → mandate status updated, admin alerted for manual follow-up.

**Backend — notifications + cron:** four new email templates (`sendAutoDebitReminderEmail`, `sendAutoDebitSuccessEmail`, `sendAutoDebitFailedEmail`, `sendMandateAlertEmail`) plus matching `NotificationService` methods (email + best-effort WhatsApp via the existing generic `sendWhatsAppText`, all fire-and-forget per the established pattern). **No new cron job** — the existing daily `jobs/installmentReminders.ts` (already scanning `dueDate`+`status` with the exact index the brief asked for) now branches on `collectionMethod`: autopay installments get an informational "this will be auto-debited" notice instead of the manual plan's "please make a payment" copy, since asking an AutoPay student to pay manually would be confusing and wrong. The 3-day reminder window doubles as the AutoPay pre-debit notice; the exact current RBI/NPCI minimum notice period was **not** re-verified against live docs this session (commonly-cited minimum ~24h, 3 days clears it comfortably) — worth confirming for real before relying on it.

**Frontend:** `InstallmentPlanModal.tsx` gained a Manual Plan / AutoPay (UPI) tab toggle (only shown before a plan exists). The AutoPay tab requires an explicit consent checkbox with the RBI-relevant consent text before the button enables — consent timestamp+IP is captured server-side at mandate creation, not just logged client-side. On success it shows the Razorpay-hosted `short_url` with a copy-link action (admin shares it with the student to complete UPI authentication on their own device — Subscriptions uses this hosted-checkout link, not the embedded `checkout.js` widget used for one-off payments). Admin payments page (`admin/payments/page.tsx`) gained an "AutoPay Mandates" panel (hidden when empty) listing every mandate with student/amount/status, visually flagging `paused`/`failed`. Admin student detail page gained an "AutoPay: {status}" badge next to Payment Summary. Student dashboard payments page gained a "Next AutoPay debit: ₹X on {date}" card when a mandate is active, plus an AUTOPAY tag on autopay-sourced installment rows — the existing Installment Schedule / Payment History tables and receipt-download links needed no structural changes, they already generalize to autopay-collected installments/payments.

**Testing — 23 new backend tests (73 total, was ~50 before this session, all passing):** `tests/mandate.test.ts` (creation, consent-required, uneven-split rejection, duplicate-mandate rejection, student-can-read-own/can't-read-others, admin-only mandate listing, cancellation, manual-plan-unaffected regression), `tests/webhook.test.ts` (signature required/rejects tampered/accepts valid, all 6 lifecycle events, and — the one the brief called out explicitly — a same-payload-twice idempotency test asserting exactly one Payment and one paid installment result). `tests/installmentReminders.test.ts` (autopay branch sends the AutoPay-specific copy and not the manual one and vice versa, respects the 24h resend cooldown, flips overdue, leaves paid installments alone — dates built relative to `Date.now()` rather than mocking the system clock, since it's the job's window math under test, not time itself). Razorpay SDK calls are mocked in `mandate.test.ts`; `webhook.test.ts` uses the **real** HMAC verification code path against a dummy secret injected via a new `tests/setup/env.ts` (Jest `setupFiles`) so the signature math itself is genuinely exercised, not mocked away.

**Verification method — matching this repo's established standard (real running app, not just unit tests):** started a local dev MongoDB (`scripts/devMongo.ts`), built+started both servers, logged in as admin, and drove the actual UI: created a course + approved student via API, opened the Installments modal, confirmed the Manual Plan path still works with zero regressions (plan generation, Mark Paid, receipt in Payments list), then the AutoPay tab — consent gating works, and submitting without real Razorpay keys correctly surfaces the graceful 503 rather than crashing. Directly inserted an `active` mandate + autopay installments into the dev DB to verify the **populated** UI states that can't be reached without real Razorpay credentials: the admin "AutoPay Mandates" panel, the per-student "AutoPay: Active" badge, and the student dashboard's "Next AutoPay debit" card all rendered correctly with real data, no console errors. **Not verified:** an actual Razorpay test-mode mandate creation → checkout → webhook round trip — blocked on the open follow-up above (real test keys + confirmed Subscriptions access).

**Phase 8 status:** was `SPOT-CHECKED OK`, manual-only. Now: manual payment flow unchanged and regression-tested; AutoPay e-mandate flow fully built, tested (mocked Razorpay), and UI-verified against a real running app; genuinely blocked only on Razorpay account access the user needs to be present for.

## Phase 8 continued — real Razorpay test-mode verification + fixed billing-day fix (2026-08-01, same day)

**Trigger:** user supplied real Razorpay API keys (first a live-mode CSV, then — after being flagged that live keys mean real money/real bank mandates — a test-mode CSV instead) and asked to verify the AutoPay flow end-to-end for real, then reported the actual problem driving this: students intentionally delaying UPI authentication to push their own due date out.

**Test-mode keys wired in, local dev only:** `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET` (`rzp_test_...`) added to `wg-backend/.env` (the root one — confirmed via `dotenv.config()` + `npm start`'s cwd that this is the file actually loaded, not the stale `wg-backend/src/.env` which has unrelated legacy values and isn't read by the app). `RAZORPAY_WEBHOOK_SECRET` left blank — no public URL available locally to register a real webhook against.

**Previous session's hard-stop resolved:** Subscriptions **is** enabled on the account. Verified by actually creating a real Plan + Subscription against the live Razorpay test-mode API (not mocked) via a seeded dummy approved student (₹30,000 pending, 3× ₹10,000 monthly installments): `POST /api/payments/mandate` returned a real `plan_...`/`sub_...` pair and `short_url`. User opened the hosted checkout, selected the UPI tab specifically (checkout also offers Card/eNACH — those are for eMandate generally, not what we're testing), authenticated with Razorpay's test-mode UPI handle, and it went through. Polled `GET https://api.razorpay.com/v1/subscriptions/:id` directly (Basic Auth with the test key) and confirmed `status: "active"`, `payment_method: "upi"`, `paid_count: 1` — the first cycle auto-charged immediately in test mode.

**Real gap surfaced by this: local DB desyncs from Razorpay without webhook delivery.** After the above, our own `Mandate.status` was still stuck on `"created"` and installment #1 still `"pending"` — the transition only happens via the `subscription.authenticated`/`subscription.charged` webhook events, which Razorpay can't deliver to a bare localhost server. **Not yet fixed or worked around** — needs either an `ngrok`-style tunnel + a real webhook registered in the Razorpay dashboard (to fully verify locally), or waiting until this is wired into a publicly-reachable environment (Render). Flagged to the user, not yet actioned — ask before setting up a tunnel.

**Real bug found and fixed — billing date wasn't actually fixed:** the user's stated problem (a student deliberately delaying UPI authentication to push their own due date later) traced to a real gap: `POST /api/payments/mandate` accepted a `startDate` field but only ever used it for the *local* `Installment.dueDate` display — it was never passed to Razorpay's `subscriptions.create`, so the **real** recurring charge date silently followed whenever the student happened to finish authenticating, not any date the admin intended. Fixed:
- `razorpayService.createSubscriptionMandate` now accepts `startAt` (unix seconds) and passes it through as Razorpay's `start_at`, which anchors every future cycle to that exact calendar date regardless of authentication timing.
- `mandate.controller.ts`: when the admin doesn't supply `startDate`, monthly plans now default to the **next occurrence of the 3rd of the month** (`DEFAULT_MONTHLY_BILLING_DAY = 3`, business default per this session — day chosen from the user's "3rd or 4th" framing, easy to change to a different single constant later if wrong), with at least `MIN_AUTH_LEAD_DAYS = 2` days' runway for the student to actually authenticate before the first charge attempt. If the 3rd is less than 2 days out, it rolls to next month's 3rd. An explicit admin-supplied `startDate` less than 2 days out is now rejected with a clear 400 instead of silently creating a mandate that's likely to fail its first cycle.
- Daily/weekly plans with no explicit `startDate` are unchanged (still start immediately after authentication) — the fixed-day concept only applies to `startDate`-less monthly plans, which is the installment-fee use case this was built for.
- **Verified against the real test-mode API**, not mocked: created a second dummy student's mandate with no `startDate` on 2026-08-01, and `GET /v1/subscriptions/:id` confirmed Razorpay's own `start_at`/`charge_at` = **2026-09-03 00:00 UTC** — the 3rd of next month, exactly as intended.
- All 23 existing backend tests still pass unchanged (they mock `createSubscriptionMandate` directly, so the new `startAt` param didn't need test updates; none of them assert on `Installment.dueDate` values, so the changed default-date logic didn't break anything).
- **Known pre-existing cosmetic quirk, not touched:** local `Installment.dueDate` values for cycle 2+ are computed via a flat `cycleLengthDays` (30 days per month), so they drift a day or two from the real calendar-month Razorpay charge date over several cycles (e.g. showed Nov 2 instead of Nov 3 for installment 3) — cosmetic display-only drift, doesn't affect the real charge date Razorpay uses. Worth fixing with real calendar-month arithmetic if it causes confusion, not done this session.

**Not committed:** all of Phase 8's AutoPay work (from both this session and the prior one) is still **uncommitted** in `wg-backend` — `git status` shows it entirely as modified/untracked files. Per this file's own standing instruction ("commit regularly — don't let work sit uncommitted"), this is worth committing soon; not done automatically this session since the user didn't ask for a commit.

**Webhook leg — now verified end-to-end, same session:** exposed the local backend via a Cloudflare quick tunnel (`npx cloudflared tunnel --url http://localhost:5001`, trycloudflare.com) — **both `ngrok`-style hostnames and `loca.lt` (localtunnel) were rejected outright by Razorpay's webhook URL field** ("hostname not allowed"), trycloudflare.com was accepted. Registered a real Test Mode webhook in the Razorpay dashboard pointed at `<tunnel>/api/webhooks/razorpay` with a generated secret, dropped the secret into `RAZORPAY_WEBHOOK_SECRET`, restarted the backend. Created a third dummy student + mandate, had the user authorize it via UPI — Razorpay delivered a real `subscription.authenticated` webhook, our signature verification passed, and `Mandate.status` flipped from `created` → `active` in the local DB automatically with zero manual intervention (confirmed via both the API response and the backend access log: `POST /api/webhooks/razorpay 200`). This is the first real (non-mocked) verification of the full mandate-creation → UPI-authorization → webhook → local-DB-sync loop.

**Caveat:** the tunnel URL is ephemeral — it changes every time the tunnel process restarts, so the webhook entry in the Razorpay dashboard will need its URL updated if the tunnel is recreated in a future session.

## Phase 8 continued — redesigned to immediate-first-debit + fixed 30-day cadence, per the real business rule (2026-08-01, same day)

**Trigger:** the fixed-3rd-of-month anchor built earlier this session turned out to be the wrong shape for the actual policy: the real rule is "first installment debits immediately when the student authorizes (which must happen same-day or within 24h of admission), then every 30 days after that" — not a fixed calendar day. Confirmed with the user that authorization is expected same-day/within-24h, which is what makes an immediate first debit safe (it doesn't reopen the delay-gaming loophole, since the window to delay is bounded).

**Changed:**
- Removed the `DEFAULT_MONTHLY_BILLING_DAY` / `nextMonthlyBillingDate` logic entirely. Default (no explicit `startDate`) is now: no `start_at` passed to Razorpay at all, so the first installment debits **immediately as part of UPI authentication itself** — this is Razorpay's native behavior (verified: `paid_count` goes from 0 to 1 the instant authentication completes, no separate delay).
- Added Razorpay's `expire_by` param (`razorpayService.ts`, `mandate.controller.ts`, `MANDATE_AUTH_EXPIRY_HOURS = 24`): if the student doesn't authenticate within 24h of mandate creation, Razorpay auto-expires the subscription so it can't be authorized later. This is what actually enforces "same day or 24h" — a real constraint at the Razorpay level, not just a UI reminder — and is what makes the immediate-first-debit design safe against the original delay-gaming complaint.
- **Exact 30-day cadence, not calendar-month:** Razorpay's own `monthly` period is calendar-month length (28-31 days) — the user was specific about "30 days," so `razorpayService.createSubscriptionMandate` now translates a `monthly` request into Razorpay's `period: 'daily', interval: 30` under the hood (local `Mandate.period` still stores/displays `'monthly'` — only the actual Razorpay API call is translated). This also incidentally fixes the previously-flagged cosmetic drift bug (local `Installment.dueDate` already assumed a flat 30 days per "monthly" cycle; now the real Razorpay charge date matches it exactly instead of drifting a day or two over several cycles).
- **Verified against the real test-mode API, not mocked:** created a 4th test mandate, confirmed `expire_by` was exactly `created_at + 24h` before authorization, then authorized it and confirmed `paid_count: 1` immediately with `charge_at` exactly `current_start + 30 days` (computed the diff directly: `30`, not "≈1 month").
- All 23 backend tests still pass unchanged.

**Real bug found and fixed by this same live test — webhook dedupe-vs-processing-order:** the `subscription.charged` webhook fired for real this time (confirming the auto-debit-marks-installment-paid leg actually delivers), but the handler threw (a Mongoose validation error — the dummy test student had no `courseId`, and `Payment.courseId` is required) and returned a `422`. Investigating exposed a real structural bug in `webhook.controller.ts`: the idempotency record (`RazorpayWebhookEvent`) was inserted **before** the event handler ran, to block concurrent duplicate deliveries — but that meant a failure *during* processing left the delivery permanently marked "already processed," so even Razorpay's own automatic webhook retry would be silently swallowed by the dedupe check and the installment would never actually get marked paid, despite the real charge having gone through. Fixed: the event dispatch is now wrapped in try/catch; on failure, the just-inserted dedupe row is deleted (rolled back) and a `500` is returned instead of `200`, so Razorpay's retry can reprocess it cleanly.

**Clean end-to-end confirmation (a 6th test mandate, this time with a real `Course` document assigned up front):** mandate created → authorized via UPI → `subscription.authenticated` and `subscription.charged` both delivered and processed successfully on the **first attempt** → installment marked `paid` with a real `razorpayPaymentId` → `Payment` record created (`upi_autopay`, correct receipt number) → student `totalPaid`/`pendingAmount` updated → all visible correctly in both the admin Payments page and the student's own dashboard. This is the full loop working with zero manual intervention. (Separately, the original bugged test mandate's webhook retry was polled for ~10 minutes without Razorpay's backoff landing in that window — not a concern, since the clean mandate above already proves the same code path end-to-end.)

**Committed and pushed:** `wg-backend` — `ced2acd` (initial AutoPay build) → `a3707fe` (this redesign + webhook fix) → merged with an unrelated concurrent roadmap-only commit from another session → pushed as `9f98839`. `wg-frontend` — AutoPay UI (mandate tab, admin mandates panel, student dashboard cards) committed as `9b55472` and pushed. Both repos' full test suites pass (73 backend, 26 frontend).

## App-wide functionality verification pass (2026-08-01, same session)

**Trigger:** user asked for a deep pass through the whole roadmap with real functionality testing, not just code review, fixing anything found, then pushing.

**Method:** full automated test suites (73 backend + 26 frontend, all passing) plus a real browser click-through against both dev servers running locally (`scripts/devMongo.ts`, backend :5001, frontend :3000 in production mode) — public site, admin panel, and student dashboard, matching this repo's established "verify against a real running app" standard.

**Verified working, no bugs found:** homepage, course catalogue (seeded via `npm run seed:courses` — was empty in this dev DB), course card data fetch + ISR revalidation, testimonials page, admin login, admin dashboard stats, the AutoPay Mandates panel + payment history on the admin Payments page, the AutoPay status badge + Payment Summary on the admin student-detail page, student login, the student dashboard's Installment Schedule (AUTOPAY tag) + Payment History, and the CRM Enquiry Pipeline board (pre-existing data from a prior session's real-phone testing still renders correctly).

**Two apparent bugs investigated and ruled out (worth recording so a future session doesn't re-chase them):**
1. Course/testimonial grids appeared completely empty right after seeding — root cause was Next.js ISR's stale-while-revalidate window (`x-nextjs-cache: STALE` → one background regen cycle → `HIT` with fresh data), not a fetch failure. Resolved itself within seconds; not a bug.
2. The course grid, and separately the login form fields, appeared empty/uninteractable immediately after navigation in browser automation. Root cause: this app intentionally delays mounting its Three.js hero background by ~900ms (documented elsewhere in this file) to avoid fighting the page's entrance animation — interacting via automation *before* that settles either lands clicks on the wrong element or catches the course grid before its `whileInView` scroll-reveal has registered the initial viewport. Waiting ~2s after navigation before the first interaction resolved both every time. Real users scrolling normally would never notice this; it's purely a fast-automation timing artifact, consistent with similar findings recorded elsewhere in this file (Phase 13 CRM drag-and-drop spot-check).

**No real app bugs found in this pass** beyond the two already fixed above (courseId-required webhook crash, webhook dedupe-rollback). Nothing further needed fixing or pushing as a result of this pass.

## Phase 13 — Admissions CRM + responsive overhaul (2026-07-28)

**Trigger:** user asked for (1) a full responsiveness pass over the whole app and (2) a complete admissions CRM with a drag-and-drop enquiry pipeline, counsellor role, and dashboard.

### The environment bug that has been wasting whole sessions — root-caused

`~/Desktop` is iCloud-synced with "Optimize Mac Storage", which **evicts files inside `node_modules`** (they show as `dataless` in `ls -lO`). Node then reads a truncated/empty file and throws something that looks like a code bug but isn't. This session alone it produced, on different attempts against *identical* source: `ERR_INVALID_PACKAGE_CONFIG` on random `next` internals, `_interop_require_wildcard._ is not a function`, `Class extends value undefined` in `mongodb/gssapi.js`, `require(...) is not a function` in `morgan`/`debug`, and `mime/types.json: Unexpected end of JSON input`. Backend `node_modules` had **9,102** evicted files, frontend **28,095**.

- **Diagnosis:** `ls -lRO node_modules | grep -c dataless`. Non-zero means this is biting you.
- **Fix that works:** `rm -rf node_modules && npm ci` (writes real local files). `brctl download` did *not* materialise them, and reading files back with `cat` was far too slow (~87 files/min).
- **A stalled `tsc` at 0% CPU for 20 minutes is this, not a slow compile.** Same for a server that starts, prints nothing, and never binds a port.
- **Permanent fix (still not done):** move the repos off `~/Desktop`, or exclude `node_modules` from iCloud. Until then expect this to recur after the Mac is idle/low on disk.
- Also found: `.git/objects/pack/pack-f2400f9b…pack is far too short to be a packfile` in the backend repo — same corruption, hitting git. `git log` can't traverse past `97ede80`. Harmless for commit/push, but history archaeology fails.

### CRM — what was built

- **Model** (`models/Enquiry.ts`): fields per the brief (name, course, education, workingStatus, mobile, email, remarks, enquiryDate, source), plus `stage`, `owner`, `createdBy`, and an embedded `stageHistory[]`. Six stages (`new_enquiry → follow_up → demo_scheduled → demo_done → admitted | cancelled`), four sources (justdial / offline_marketing / website / google_maps). Indexed on `{stage, updatedAt}`, `{owner, stage}`, `mobile`, `source`, `enquiryDate`, `createdAt`.
- **Timeline:** every stage change — whether dragged on the board or changed via the edit form — appends `{fromStage, toStage, changedBy, changedAt, note}`. Creation seeds the first entry, so the timeline is never empty.
- **Ownership:** one helper (`scopeToUser`) gates every read, and one (`assertCanMutate`) gates every write, so the rule can't drift per-route. Counsellors see and edit only their own enquiries; admins see everything. `role: 'student'` is hardcoded on OAuth/self-signup and `owner` is ignored from counsellor payloads — a counsellor cannot assign work to someone else or escalate.
- **Endpoints:** `GET/POST /api/enquiries`, `GET/PUT/DELETE /api/enquiries/:id` (delete is admin-only), `PATCH /api/enquiries/:id/stage`, `GET /api/enquiries/stats`, and `GET/POST/PUT/DELETE /api/users` for staff accounts.
- **Deleting a counsellor reassigns their enquiries to the acting admin** rather than orphaning them (`owner` is required, so orphans would fail validation later). Last-active-admin cannot be deleted, demoted, or deactivated.
- **Duplicate guard:** a second *active* enquiry on the same mobile is rejected; once the first is `admitted`/`cancelled`, re-enquiry is allowed.
- **Frontend:** `@dnd-kit` (not react-beautiful-dnd — that's unmaintained and doesn't support React 19). Optimistic move with a pre-mutation snapshot restored on API failure. Kanban at `/admin/crm/pipeline` and `/counsellor/pipeline`, dashboard at `/admin/crm` and `/counsellor`, staff management at `/admin/crm/users`, and a separate counsellor area with its own layout.
- **`conversionRate` is admitted ÷ (admitted + cancelled)**, i.e. measured against *closed* enquiries. Counting still-open ones as failures would understate it badly early on.

### Responsiveness

- **The "pages look zoomed in on mobile" complaint was two real causes**, both fixed: no explicit `viewport` export (added to `app/layout.tsx` with `width=device-width, initialScale: 1`), and inputs under 16px, which makes **iOS Safari zoom the whole page on focus** — now forced to 16px under 640px. Pinch-zoom deliberately left enabled (`maximumScale`/`userScalable` untouched) since disabling it is an accessibility failure.
- **The horizontal-scroll complaint was a flexbox bug:** `admin/` and `dashboard/` layouts had `flex-1` main areas with no `min-w-0`, so a wide table or the kanban forced the *whole page* sideways instead of scrolling internally. Added `min-w-0` to both (and the new counsellor layout).
- Heading sizes were hand-tuned per page (`text-4xl` here, `text-3xl` there, some with no mobile step-down) — that inconsistency was what made pages read as differently scaled. Replaced with three `clamp()`-based classes: `.heading-hero`, `.heading-section`, `.heading-panel`.
- `container-custom` narrowed 1400px → 1280px; at 1400 the line length was uncomfortably wide on large monitors next to the narrower pages.
- Also: `overflow-x: hidden` guard on `html`/`body` (decorative blur orbs deliberately overflow their sections), three admin tables wrapped in `.scroll-x` with a `min-w`, four modals given `max-h-[90dvh]` + internal scroll (they ran off-screen on landscape phones), navbar drawer capped at `min(320px, 85vw)` (was a flat 300px — 20px of page left on a 320px device), navbar active-state now matches nested routes, and a 44px minimum touch target under `@media (pointer: coarse)`.

### Testing — 76 tests, all passing (was 19)

- **31 new backend tests** (`tests/crm.test.ts`): lifecycle, timeline attribution, validation, duplicate guard, and the full role matrix (counsellor scoping, cross-owner denial, admin-only delete, students locked out entirely, unauthenticated 401s).
- **13 new frontend tests** (`src/__tests__/crm-pipeline.test.tsx`): all six columns render, cards land in the right column, search across name/mobile/course, stage filter + clear, detail timeline, admin-vs-counsellor delete affordance, date prefill, the exact four sources, and validation.
- **Two pre-existing bugs found by writing these:**
  1. `tests/cms.test.ts` asserted `401` for an authenticated-but-unauthorised student. `authorize()` was deliberately changed to `403` back in the OAuth commit (`4ce630c`) and this assertion was left stale — it had been failing ever since. Fixed the assertion, not the middleware; the 403 is correct.
  2. **The rate limiters had no test bypass**, so a full Jest run tripped `authLimiter` (10 logins/15min, all from loopback) partway through and every subsequent request failed as unauthenticated. Added `skip: () => NODE_ENV === 'test'` to all three limiters — they stay fully active in dev and production.
- **My own accessibility bug, caught by the tests:** every `<label>` in the CRM form and filter panel was unassociated with its control (no `htmlFor`/`id`), so screen readers announced nothing and clicking a label didn't focus the field. Fixed the components rather than loosening the tests.

**Not verified this session:** no browser automation was available (the Chrome MCP server disconnected), so **the drag-and-drop gesture itself has not been exercised in a real browser** — the stage-move API it calls is covered by tests, and the board renders correctly under jsdom, but an actual pointer-drag across columns is untested. Same for the visual responsive breakpoints: the CSS/markup fixes are verified by inspection and build output, not by looking at rendered pages at each width. Worth 10 minutes with a browser.

### Browser spot-check of drag-and-drop — DONE (2026-07-28, follow-up session, Windows checkout)

**Result: pointer drag-and-drop works end-to-end.** Logged in as admin on `/admin/crm/pipeline` against a real running app (local dev MongoDB via `scripts/devMongo.ts`, backend on 5001, frontend on 3000), created a test enquiry, and dragged it across columns with real mouse events (not just the stage-move API in isolation):
- `New Enquiry → Demo Scheduled` and `Demo Scheduled → Admitted` both succeeded — card moved visually, `PATCH /api/enquiries/:id/stage` returned 200, and the card's Stage Timeline recorded both transitions with correct timestamp + actor attribution.
- **Found:** dragging near the board's right edge triggers horizontal auto-scroll, which can make a drop land one column past the visual target. Likely a browser-automation artifact (single-jump synthetic drag vs. a real user's continuous mouse tracking) rather than a genuine app bug — not confirmed with a human hand on a real mouse, so treat as an open question, not a fixed defect.
- A short reverse-direction (right-to-left) drag attempt once didn't register as a drag at all (no network request fired) — same likely-automation-artifact caveat.
- First delete attempt froze the automated browser tab — confirmed root cause: the CRM detail modal's delete button uses a native `window.confirm()` (unlike the rest of the admin, which uses the app's own `ConfirmDeleteModal` component), and Claude's browser tools can't dismiss native dialogs. Worked around by stubbing `window.confirm = () => true` via injected JS before clicking delete, which worked cleanly. **Worth swapping this one spot to `ConfirmDeleteModal` for consistency and so it doesn't block automated/E2E testing going forward.** Test enquiry ("Test Draguser") was successfully deleted afterward — local dev DB is clean, no leftover data. This was local dev data only, not production.

**Real bug found and worked around (not yet fixed in code):** local backend startup crashed with `TypeError: OAuth2Strategy requires a clientID option`. `src/config/passport.ts` constructs the `GoogleStrategy` unconditionally at module load, but `src/config/env.ts` treats `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` as optional (`z.string().default('')`), and `passport-oauth2` throws if `clientID` is empty. Worked around locally with dev placeholder values in `.env` (gitignored, not committed) — **not a real fix**. Anyone else setting up this repo fresh without Google OAuth credentials configured will hit the same crash. Should guard the `passport.use(new GoogleStrategy(...))` call behind a check for non-empty credentials, or make Google sign-in registration conditional/lazy.

**Environment note:** this was run from a **second, Windows-based checkout** (`C:\Users\rohn1\Desktop\Rohan\webigeeks`), separate from the Mac environment (`~/dev/webigeeks`) referenced elsewhere in this file's "Critical context" section. `node_modules` on this checkout was stale relative to `package.json` (missing `passport`/`passport-google-oauth20`) and needed a plain `npm install` before the build would pass — not the iCloud-eviction issue described above, just a checkout that hadn't been `npm install`-ed since the CRM/OAuth work landed. This file's Mac-specific operational notes (`brew services`, `~/dev` paths) don't apply here; if this Windows checkout becomes a regular second dev environment, worth adding its own gotchas section rather than assuming the Mac notes cover it.

### Real-phone testing round — four real bugs no amount of desktop-browser automation would have caught (2026-07-28, same Windows checkout)

**Trigger:** user tested the CRM pipeline on an actual phone (first over LAN, then on production) instead of relying on the desktop-browser spot-check above. Every one of the four bugs below only exists as a symptom of a real mobile browser (dynamic address bar, on-screen keyboard, real touch input) — none of them were visible in desktop Chrome, and one (the vertical pipeline layout) was a deliberate design change made in direct response to this testing, not just a bug fix.

**0. Mobile UX request, implemented:** pipeline stages now stack as full-width vertical sections below `lg` instead of a horizontal scroll-snap Kanban row (`PipelineBoard.tsx`, `PipelineColumn.tsx`) — the user's own suggestion, and a better fix than sensor-tuning alone: on mobile there's now no horizontal scroller to fight the drag gesture at all, since the page's native vertical scroll and the drag's vertical movement are the same axis. Side-by-side columns are unchanged at `lg:`+.

**1. Drag-and-drop touch reliability:** root cause was a single distance-only `PointerSensor` plus `touch-action: none` on cards — any finger movement (including an attempted scroll) was immediately claimed as a drag, with the browser's native scroll blocked outright. Fixed with dnd-kit's documented pattern: `MouseSensor` (distance-based, desktop) + `TouchSensor` (`delay: 200ms, tolerance: 8`, touch), `touch-action: manipulation` instead of `none` so scrolling works until a drag actually activates, and scroll-snap disabled while dragging (it was fighting dnd-kit's auto-scroll, causing the reported column-jump).
   - **Auto-scroll speed needed escalating three times** (12 → 24 → 48 → 120, i.e. plain default → 2x → 4x → 10x) before the user stopped reporting it as slow. Verified directly against `node_modules/@dnd-kit/core/dist/core.esm.js`: `acceleration` scales `scrollContainer.scrollBy()` linearly with no intermediate cap (called every 5ms), so each multiplier claim above is exact, not approximate. **Not yet confirmed by the user that 120 actually feels right** — they moved on to reporting the sidebar bug below before confirming. Check this first if resuming.

**2. CORS rejecting the phone entirely ("Internal Server Error" on login):** testing from a phone requires the browser to reach the backend by something other than `localhost` (meaningless from the phone's perspective). Solved by routing through the existing production pattern — relative `/api` calls proxied server-side by `next.config.ts`'s rewrite (`BACKEND_URL` env var) — rather than exposing the backend on the LAN directly. That rewrite forwards the browser's *original* `Origin` header straight through to the backend, so the backend's CORS allow-list (`localhost:3000` only) rejected it. Fixed in `wg-backend/src/app.ts`: private-LAN origins (`192.168.x.x`, `10.x.x.x`, `172.16-31.x.x`) on port 3000 are now allowed, but **only when `NODE_ENV === 'development'`** — production's allow-list is untouched.

**3. Modals "ruined" until pinch-zoomed out — a two-step fix, and the first attempt was wrong:**
   - First symptom (New Enquiry form + enquiry detail popup specifically): content cut off / misaligned at normal zoom, looked fine once the user pinch-zoomed out. Root cause: both used plain `vh` for `max-h`, while every *other* modal in the app already used `dvh` (a Phase-12/13-era fix that these two newer CRM modals didn't get). Mobile `vh` is computed against the browser's *expanded* viewport (as if the address bar were hidden) — taller than what's actually visible when the bar is showing. Switched to `dvh`.
   - **That traded one bug for another**: `dvh` is *dynamic* — it recalculates live as the address bar toggles or the on-screen keyboard opens, so the modal would visibly resize while the user was mid-interaction with it (typing a field, scrolling). Reported as "popups still resizes." Real fix: `svh` (the *stable*, smallest-possible-viewport unit — doesn't change once the page has loaded), applied universally across **every** modal/popup in the app (`EnquiryFormModal`, `EnquiryDetailModal`, `InstallmentPlanModal`, `ConfirmDeleteModal`, `RecordPaymentModal`, `ExitIntentPopup`, `InquiryPopup`), not just the two originally reported — the same dvh-resize bug would have hit all of them eventually.
   - **Lesson for next time a modal "looks broken only on a phone, fine when zoomed out":** check `vh` vs `dvh` vs `svh` first, in that preference order for anything that stays open during user interaction (dialogs, drawers) — `dvh` is only safe for content that isn't held open across a chrome-toggle or keyboard-open event.

**4. Mobile sidebar cut off at the bottom + background scroll bleed-through:** a real phone screenshot showed the student dashboard's mobile sidebar drawer with its Logout button cut off below the visible screen. Same root cause as bug #3, just not caught by the modal audit since a sidebar isn't a modal: `<aside>` used Tailwind's `h-screen` (=`100vh`) across all three role layouts (`admin/layout.tsx`, `dashboard/layout.tsx`, `counsellor/layout.tsx`). Fixed to `h-svh` in all three (stable, matches the modal reasoning above — a sidebar held open during interaction shouldn't use `dvh` either). Separately, none of the three layouts locked body scroll while their mobile drawer was open — the public `Navbar.tsx` mobile menu already did this, but it was never carried over to the three dashboard-style layouts — so a scroll gesture anywhere over the open drawer fell through to the page behind it. Added the same `document.body.style.overflow = 'hidden'` pattern to all three.

**Commits, `wg-frontend`:** `273b3a6` (sensors/vertical layout/grid conversions) → `a8f4c70` (dvh, autoScroll 2x) → `105e7da` (svh universal, autoScroll 4x) → `79a89e0` (autoScroll 10x) → `6670735` (sidebar h-svh + body-scroll-lock). **`wg-backend`:** `d9b371c` (dev-only LAN CORS allowance).

**Meta-lesson:** the desktop-browser automated spot-check earlier in this file found real things, but real-device testing found four *more* bugs that automation structurally cannot reproduce — dynamic mobile address-bar behavior, on-screen keyboard viewport changes, and genuine touch input. If a future session has access to a real phone (or even just resizing a real browser's viewport and toggling its own chrome), treat that as a distinct, necessary testing pass — not a redundant one — before calling any mobile-facing UI done.

### Sticky sidebars/headers were broken app-wide by one CSS rule (2026-07-29, FIXED + BROWSER-VERIFIED)

**Trigger:** user reported that on the admin dashboard the sidebar scrolls up with the page instead of staying put.

**Root cause: `body { overflow-x: hidden }` in `globals.css`.** `overflow-x: hidden` makes the element a scroll container in *both* axes (per spec, the other axis's `visible` computes to `auto`). Every `position: sticky` descendant then resolves against the **body's** scrollport instead of the viewport — and the body's scrollport never scrolls, the viewport does — so the sticky element just scrolls away with the content. This silently disabled sticky positioning across the whole app: the `lg:sticky` sidebar **and** the `sticky top-0` header in all three role layouts (`admin/`, `dashboard/`, `counsellor/`).

- **Fix: `overflow-x: clip` instead.** `clip` does identical clipping but does *not* create a scroll container, so sticky keeps working. One line, in `body` only.
- **`html`'s `overflow-x: hidden` was NOT the culprit and is deliberately left alone** — the UA propagates root-element overflow to the viewport and the used value on the element itself becomes `visible`. That also means the original anti-horizontal-scroll guard still holds in browsers too old for `overflow: clip` (Safari <16); the `body` rule was always belt-and-braces on top of the `html` one.
- **If a `position: sticky` element ever "just doesn't stick" again, check every ancestor for `overflow: hidden/auto/scroll` first** — the property that breaks it is usually nowhere near the element that looks broken.

**Verified in a real browser, with a control.** No browser MCP was connected this session, so one was wired up ad hoc: `puppeteer-core` driving the installed system Chrome (`/Applications/Google Chrome.app`) headless against the real running app — real admin login, real `/admin/analytics` page, real scroll to y=900 at 1280×800. Measured `getBoundingClientRect().top` on the `<aside>` and `<header>` before and after scrolling:

| | at top | scrolled to y=900 |
|---|---|---|
| with fix (`clip`) | aside 0, header 0 | aside **0**, header **0** — pinned |
| control (`hidden` re-injected live) | aside 0, header 0 | aside **-900**, header **-900** — scrolled off |

The control leg matters: re-injecting the old rule into the same live page reproduced the reported bug exactly, which is what makes the pass meaningful rather than just "looks fine". **This pattern — measure, then re-inject the old rule and prove the measurement catches it — is worth reusing for any future CSS fix.** `puppeteer-core` + system Chrome needs no Chromium download and is a fine stand-in when the browser MCP is unavailable.

### FOOTGUN: a local production-mode build talks to the PRODUCTION backend (found 2026-07-29, NOT fixed)

Found while setting up the browser test above. `next.config.ts` does `const BACKEND_URL = process.env.BACKEND_URL || "https://wg-backend-dgtd.onrender.com"`, and `BACKEND_URL` is not set anywhere locally — so `npm run build && npm start` on this machine proxies `/api/*` **straight to the live Render backend and Atlas database**. An admin session on `localhost:3000` is editing production data, with nothing in the UI to indicate it.

- **Rewrites are baked into `.next/routes-manifest.json` at BUILD time**, not read at runtime — setting `BACKEND_URL` before `npm start` does nothing. It must be set for `npm run build`. (Cost a confused debugging cycle: the runtime env var appeared to be ignored, because it was.)
- To run fully locally: `BACKEND_URL=http://localhost:5001 npm run build && npm start`. Confirm with `grep -o "onrender[^\"]*\|localhost:5001[^\"]*" .next/routes-manifest.json`.
- **Suggested fix, not applied (user hasn't decided):** default to `http://localhost:5001` when `NODE_ENV !== 'production'` so the production URL is opt-in rather than the default.
- Incident note: one admin login attempt did reach production this way before it was noticed. It failed with 401 (production's admin password is not the local `Admin@123`) — nothing read or changed, one slot of the production auth rate limit consumed.

---

## Phase Status Overview

| Phase | Name | Status | Notes |
|---|---|---|---|
| 13 | Admissions CRM + Responsive | DONE, REAL-PHONE VERIFIED | Built 2026-07-28. 76 tests passing. Browser-automation spot-check, then a real-phone round found 4 more bugs invisible to any browser automation: CORS-vs-LAN, vh→dvh→svh modal saga, mobile sidebar cut off (h-screen→h-svh) + missing body-scroll-lock — see Phase 13 sections. Pipeline is now a vertical stack below `lg` (was always horizontal). **2026-07-29:** sticky sidebars/headers were broken app-wide by `body { overflow-x: hidden }` — fixed to `overflow-x: clip`, verified in a real browser with a control. **Open:** user hasn't yet confirmed autoScroll acceleration:120 (10x) actually feels right. Found (unfixed): `config/passport.ts` crashes backend startup when Google OAuth env vars are unset; a local prod-mode build silently proxies to the *production* backend (see footgun section). |
| 1 | Authentication, Security, RBAC | AUDITED, FIXED | Audited 2026-07-25: fixed a mass-assignment vuln in `PUT /api/students/:id`, added rate limiting to change-password, removed JWT from response bodies, added zod validation to the student profile route. **2026-07-26**: fixed a cross-site auth cookie bug found in production (see Phase 12) and implemented Google OAuth 2.0 sign-in (passport-google-oauth20) — see Phase 1 section below. |
| 2 | Public Website | SPOT-CHECKED OK, CMS WIRED | Testimonials/Gallery/Blog now read real backend data (2026-07-25) — see CMS section below. Home/Courses/About/Contact/Register still only smoke-tested (2026-07-24). |
| 3 | Courses Module | SPOT-CHECKED OK | Admin course list (10 courses) + edit form load correctly. |
| 4 | Student Registration | AUDITED | Audited 2026-07-25 — structurally sound; only defect was the mass-assignment issue shared with Phase 1, now fixed. |
| 5 | Student Dashboard | AUDITED, OK | Audited 2026-07-25 — every page (Overview, Course, Payments, Documents, Notifications, Profile) reviewed against its source; all fully wired to real endpoints, no mock data, no broken links. |
| 6 | Admin Dashboard | SPOT-CHECKED OK, CMS WIRED | Home dashboard, Students, Leads, Payments, Notifications, Settings load without errors. Testimonials/Gallery/Blogs admin CRUD built out 2026-07-25 (previously non-functional stubs) — see CMS section below. |
| 7 | Communication (Email/WhatsApp) | EMAIL DONE, WHATSAPP NOT STARTED | **2026-07-26**: email verified end-to-end in production with a real send (`✉️ Email sent to ...` in Render logs) — required upgrading Render to the Starter plan, since Render blocks outbound SMTP ports on the free tier. Notification sends were also made fire-and-forget so a flaky SMTP provider can't block API responses (see Phase 12). WhatsApp Cloud API onboarding still blocked by Meta "Onboarding failure" (CSP/fetch error in embedded Quickstart flow) — unresolved, requires the account owner present (Meta business identity/2FA). |
| 8 | Payments | AUTOPAY DONE — FULL LOOP VERIFIED LIVE, COMMITTED, PUSHED | **2026-08-01**: real test-mode keys wired in (local dev only); Subscriptions confirmed enabled. Redesigned per the actual business rule: first installment debits immediately on UPI authorization, each next one exactly 30 days later, 24h authorization window enforced via Razorpay's `expire_by`. Full loop — mandate creation → UPI authorization → webhook → Payment record → student balance update → visible in both admin and student UI — verified end-to-end against the real Razorpay test-mode API with zero manual intervention. A real webhook dedupe/retry bug found and fixed along the way. Committed and pushed (backend `9f98839`, frontend `9b55472`). See Phase 8 sections below. |
| 9 | Analytics | DONE | Completed 2026-07-24. Backend + frontend build clean, verified against real running app with real data via browser. |
| 10 | Performance/SEO/Accessibility | SEO SCORE 7.8/10 (target 8.0), C4+FAQ SHIPPED TO PROD, ACCESSIBILITY REAL FIXES STARTED | **2026-08-06** (two sessions): technical SEO taken from 3.9/10 to ~8.5-9/10 and deployed — structured data, Gurugram-targeted metadata, fixed sitemap/robots, code-split a large client bundle (real Lighthouse: Performance 58->87). Growth phase shipped 12 real location landing pages, corrected fabricated About-page content, fixed a real Navbar animation bug. **2026-08-07** (three sub-sessions, see Phase 10 continued below): fresh 31-URL audit found and fixed a sitewide Open Graph/canonical bug + FAQ schema gaps (score 6.9->7.2); shipped C4 (course metadata) and the FAQ schema fix to the real production database for the first time this project (7.2->7.8); a real user-run Google PageSpeed Insights report then drove genuine accessibility fixes — heading order, missing accessible names, a sitewide WCAG-AA contrast failure, and a www->non-www redirect. Full backlog: `webigeeks/MASTER_TASK_BOARD.md` (technical, now includes H8-H11) and `webigeeks/GROWTH_BACKLOG.md` (growth/content). Remaining: no full WCAG audit yet, just the issues this specific PageSpeed report surfaced. |
| 11 | Testing | IN PROGRESS | Started 2026-07-25 — Jest+supertest+mongodb-memory-server backend suite and Vitest+RTL frontend suite added, scoped to auth/RBAC and the new CMS routes/pages. Not yet app-wide coverage. |
| 12 | Deployment | DONE | **2026-07-26**: deployed to production — frontend on Vercel + custom domain `webigeeks.com`, backend on Render (Starter plan), DB on MongoDB Atlas. See Phase 12 section below for the real bugs hit and fixed along the way. |

---

## Phase 12 — Deployment, and the auth bugs it surfaced (DONE, 2026-07-26)

**Trigger:** user asked to deploy the site to production, then to fix a custom domain, then reported registration/login "not working" on the live site — three real, separate bugs were found and fixed this way that never showed up in local dev.

**Infra:**
- Backend → Render (`wg-backend`, Starter plan, Oregon), auto-deploys on push to `main`.
- Frontend → Vercel (`wg-frontend`), auto-deploys on push to `main`, custom domain `webigeeks.com` + `www.webigeeks.com` (GoDaddy DNS, A records → `76.76.21.21`).
- Database → MongoDB Atlas (user-provided cluster), replacing the local `mongodb://localhost:27017` dev connection.
- Railway was tried first for the backend but its free trial had expired and required a paid plan before creating any project — switched to Render instead.

**Bug 1 — TypeScript build failing only on Render, not locally:** `NODE_ENV=production` set as a build-time env var made `npm install` skip devDependencies (typescript included), so Render's build fell back to some other/newer `tsc` that rejected the project's `tsconfig.json` (`moduleResolution: "node"`, `baseUrl`, non-relative `paths` — all since deprecated/removed in newer TypeScript). Fixed by changing the Render build command to `npm install --include=dev && npm run build`.

**Bug 2 — admin registration/notifications appeared broken (actually just very slow):** Render's containers have no outbound IPv6 route; Gmail's SMTP server resolves to an IPv6 address first, so the connection attempt failed with `ENETUNREACH`, and other attempts hung for nodemailer's full 2-minute default timeout — the registration endpoint awaited that before responding, so it looked hung/broken even though it eventually succeeded. Fixed two ways: forced the SMTP transport to IPv4 with 10s timeouts (`config/email.ts`), and made non-critical notification sends (welcome email, password reset, admission approved/rejected, new lead, payment received) fire-and-forget instead of blocking the response — a real design improvement independent of the network issue. Separately, Render was found to fully block outbound SMTP ports on the free tier (changelog-confirmed) — upgrading to Starter was required for email to actually send at all, not just fail fast.

**Bug 3 — login succeeded (200 + Set-Cookie) but every subsequent request looked unauthenticated:** two stacked causes. First, the auth cookie was `SameSite=Lax` while frontend (`webigeeks.com`) and backend (`wg-backend-dgtd.onrender.com`) are different domains — fixed by making it `SameSite=None; Secure` in production (`auth.controller.ts`, `authCookieOptions`). That alone wasn't enough: even with `SameSite=None`, the cookie is *third-party* from the browser's perspective (different registrable domains), and Chrome's third-party cookie blocking silently drops it regardless of `SameSite`. **Real fix:** added a Next.js rewrite (`next.config.ts`) proxying `webigeeks.com/api/*` to the Render backend server-side, so the browser only ever talks to `webigeeks.com` and the cookie becomes first-party. This meant `NEXT_PUBLIC_API_URL` had to become a relative `/api` for browser calls — which in turn broke server-side data fetching at build time (SSG has no running proxy to resolve a relative URL against), fixed by centralizing the URL logic in a new `frontend/src/lib/apiBaseUrl.ts` that picks a relative path in the browser and the real backend URL (`BACKEND_URL` env var) server-side, imported by all 11 files that previously read `NEXT_PUBLIC_API_URL` directly.

**Also fixed/added this session:**
- Production refuses to boot with the well-known default `ADMIN_DEFAULT_PASSWORD` (`Admin@123`) — this validation already existed in `config/env.ts`; deploying just surfaced it for the first time. Real password generated and set as a Render env var, not committed anywhere.
- Google OAuth 2.0 sign-in implemented end-to-end and verified working: `passport-google-oauth20` strategy (`backend/src/config/passport.ts`), `GET /api/auth/google` + `/api/auth/google/callback` routes, `User` model now supports Google-only accounts (`googleId` field, `password` conditionally required, auto-links to an existing email/password account by email). The frontend already had an unused `GoogleSignInButton.tsx` component from an earlier, never-finished pass — wired it into both `/login` and `/register`. Google's OAuth callback URL had to point at `webigeeks.com/api/auth/google/callback` (proxied), not the Render URL directly, for the same first-party-cookie reason as Bug 3 — the Google Cloud Console "Authorised redirect URIs" needed updating to match.
- Removed a duplicate logo on `/login` and `/register` (a mobile-only logo block repeated the navbar's logo directly above the form).

**Known follow-ups:**
- WhatsApp notifications remain unconfigured (env vars blank) — separate from this session's work, see Phase 7.
- No CI/CD beyond Render/Vercel's own auto-deploy-on-push — no test gate before deploy.
- The `~/Desktop` iCloud sync issue (see gotchas above) cost significant time this session re-installing corrupted `node_modules`/`.next` — worth fixing properly (move the repo, or exclude `node_modules`/`.next` from sync) before the next long session.

---

## Checkpoint — full end-to-end test pass on production (2026-07-26)

**Trigger:** after all of Phase 12's fixes, did a full click-through of the live site (`webigeeks.com`) to verify nothing was still broken, using real browser automation (not just curl) — registration, login, logout, admin panel, lead capture, session persistence.

**Result: everything tested passed, no new bugs found.** Specifically verified live:
- Public pages (Home, Courses, About, Testimonials, Gallery, Blog, Contact) all render without errors.
- Contact page lead form: submitted a real test lead → confirmed it appeared correctly in `/admin/leads` with all fields (name, contact, course, source, status, date). This exercises the full path: frontend form → proxied API call → backend → MongoDB Atlas → admin read.
- Student registration → auto-login → `/dashboard` redirect, sidebar nav (Overview/Profile/My Course/Payments/Notifications/Documents) all load.
- Logout correctly clears the session; navigating back to `/dashboard` afterward correctly bounces to `/login?redirect=...` (confirms the auth-cookie fix from earlier in Phase 12 is holding up, not just working once).
- Admin login, `/admin/leads`, `/admin/students` (shows real pre-existing student records correctly), `/admin/courses` (correctly empty).
- All test data created during this pass (one lead, one student account) was deleted from Atlas afterward — production data untouched.

**Two things flagged, explicitly NOT bugs, decision pending from the user (ask before touching):**
1. **Courses/Gallery/Blog are empty in production** — fresh Atlas DB, nothing seeded. Testimonials already has real entries (someone added them via admin already). Backend has an existing `npm run seed:courses` script that's never been run against production. Gallery/Blog need real photos/articles from the user — not something to fabricate.
2. **Contact form's "Course of Interest" `<select>` is `required` but fails silently on submit if left on the placeholder** — no custom error shown, relies entirely on the browser's native validation tooltip. Whether this is worth a custom-error fix, given native tooltips do work for real users, is genuinely unclear — asked the user, got interrupted mid-question, **no decision made yet**. Don't assume either way; ask first.

**If resuming this exact spot from a different machine:** both repos are clean and fully pushed (`wg-backend` @ `55b2698`, `wg-frontend` @ `73b4e3f`, at time of writing) — `git pull` on both gets you current. No local-only work exists anywhere.

---

## Phase 1 audit + CMS wiring — Security hardening, Testimonials/Gallery/Blog (2026-07-25)

**Trigger:** continuing the roadmap in autonomous mode — picked the highest-value concrete work: audit Phases 1/4/5 (all marked NOT AUDITED) and close the previously-flagged gap where Testimonials/Gallery/Blog had full backend CRUD but zero frontend wiring (public pages hardcoded, admin pages non-functional stubs).

**Security fixes (`wg-backend`):**
- **Mass-assignment vuln fixed**: `PUT /api/students/:id` (`student.controller.ts`) previously did `Student.findByIdAndUpdate(id, { $set: req.body })` for a logged-in student's own record with no field allowlist — a student could set `status: 'approved'`, `isProfileLocked: false`, `totalPaid`, `admissionId`, etc. on themselves. Fixed with `pickSelfEditableFields()`, an explicit allowlist; admins still get full-field access. **Verified live**: registered a test student, attempted to self-set `status`/`isProfileLocked`/`totalPaid`/`admissionId` via the API — all silently ignored; a legitimate field (`fullName`) still went through.
- `PUT /api/auth/change-password` was missing the `authLimiter` that `verify-password` already had — added.
- Login/register/reset/change-password no longer return the JWT in the response body (cookie is the sole auth channel) — confirmed via `wg-frontend/src/lib/api.ts` that the frontend never read the body token, so this was pure unnecessary XSS exposure with nothing depending on it.
- Added a zod validation schema (`validations/student.validation.ts`) to the student profile route, matching the existing `auth.validation.ts` pattern (previously relied solely on Mongoose `required` checks).
- Added the backend's first ESLint config (`.eslintrc.json` — legacy format, since the installed ESLint is 8.57.x, not flat-config-native) — `npm run lint` was previously a silent no-op with zero config despite the deps being installed. Fixed the handful of findings (unused imports, one `no-namespace` false-positive on the standard Express `Request` type-augmentation pattern).

**CMS wiring (`wg-frontend` + `wg-backend`):**
- Public `/testimonials`, `/gallery`, `/blog`, `/blog/[slug]` now fetch real data server-side via `lib/{testimonials,gallery,blog}.ts` (same ISR pattern as the existing `lib/courses.ts` — ISR revalidate 60s), replacing hardcoded placeholder arrays. Empty states added for when there's no content yet.
- Admin `/admin/testimonials`, `/admin/gallery`, `/admin/blogs` — built out full list/create/edit/delete UIs (previously static unwired stubs), reusing the existing `admin/courses` list-page and `ConfirmDeleteModal` conventions.
- Backend: added `GET /admin/all` (testimonials, gallery) and `GET /admin/all` + `GET /admin/:id` (blogs) admin-only listing endpoints, since the public endpoints filter to `isActive`/`isPublished` only — mirrors the existing `courses/admin/all` pattern.
- `next.config.ts`: added `images.remotePatterns` for `res.cloudinary.com` so gallery/blog/testimonial images can use `next/image` (previously unset — no remote image rendering was configured anywhere in the app).
- **Verified live end-to-end** via browser + API: created a real testimonial through the admin UI, confirmed it appeared on the public page after ISR revalidation; created a blog post via API (Cloudinary not configured in this dev environment, so the UI upload step itself couldn't be exercised — see below) and confirmed it rendered correctly on both the admin edit page and the public detail page including cover image, tags, and metadata.

**Known limitation — Cloudinary not configured in this environment:** image uploads (`DocumentUploadField` → `/api/upload/image`) return a graceful 503 rather than crashing, by existing design (`assertCloudinaryConfigured`), but this means the gallery/testimonial/blog *upload* UI paths themselves weren't exercised end-to-end here — only verified via direct API calls with a pre-existing image URL. Needs real Cloudinary credentials in `.env` to fully verify the upload flow.

**Dev environment note:** this checkout had no `.env` and no local MongoDB (this appears to be a fresh machine/checkout, distinct from the one referenced elsewhere in this file). Set up a local-only dev MongoDB via `mongodb-memory-server` (`scripts/devMongo.ts` — run with `npx ts-node scripts/devMongo.ts`, binds to `mongodb://127.0.0.1:27017/webigeeks`, persists to `.devdata/` which is gitignored) plus a dev-only generated `JWT_SECRET`. This is separate from the `mongodb-memory-server` instance the test suite spins up per-run.

**Frontend lint — pre-existing debt, out of scope this pass:** `npm run lint` in `wg-frontend` has 55 pre-existing errors (confirmed via `git stash` against the last committed baseline, before this session's changes) — mostly `@typescript-eslint/no-explicit-any` in `catch` blocks and a stricter `react-hooks/set-state-in-effect`/`react-hooks/purity` ruleset flagging the app's universal `useEffect(() => { fetchX() }, [])` data-fetching pattern (used in `useAuth.ts`, every dashboard/admin page, and `Math.random()` in the Three.js scene). New CMS files follow the same established convention rather than inventing an inconsistent one-off style. Fixing this properly means touching core auth/rendering code across ~15 files — flagged as Phase 10/11 cleanup, not attempted here to avoid destabilizing critical paths for a lint-only pass.

**Explicitly not attempted (need the user present):**
- Phase 7 WhatsApp Cloud API onboarding — blocked on an interactive Meta Business account flow tied to the user's own identity/2FA.
- Phase 12 Deployment — needs a hosting/infra decision and production secrets, a business call.

---

## Phase 9 — Analytics (DONE, 2026-07-24)

**Scope:** Charts, Reports, Conversion Analytics, Revenue Analytics, Student Analytics.

**What was built:**
- Backend: refactored `analytics.routes.ts` logic into `analyticsService.ts` + `analytics.controller.ts`. Added `GET /api/analytics/students` (status/payment-mode/gender breakdown, dues, enrollment trend). Extended `/leads` with `conversionBySource` + `monthlyConversion`. Added new `GET /api/analytics/revenue/payment-methods` endpoint (kept `/revenue` itself backward-compatible as a plain array — see bug note below).
- Frontend: new `/admin/analytics` page using `recharts` (chosen over extending the app's existing dependency-free custom `BarChart` component, per explicit decision — trade-off: ~500 new packages incl. a few high-severity npm audit advisories in transitive deps, but full line/pie chart support). Added "Analytics" sidebar link. 8 charts covering all four sub-areas.

**Bug found and fixed during this work:** My first pass changed `/analytics/revenue`'s response shape from a plain array to `{byMonth, byPaymentMethod}`, which broke the **real, already-existing** admin dashboard home page (`admin/page.tsx`), which consumes `/analytics/revenue` directly as an array (`revenue.find(...)` → `TypeError: b.find is not a function`, blank dashboard). This only surfaced because the git-history discovery above meant my first implementation pass was built against a bare-boilerplate baseline that didn't have this real consumer. Fixed by keeping `/revenue` as a plain array and moving the payment-method breakdown to its own endpoint. **Lesson: always check real running-app consumers of an endpoint before changing its response shape, not just git history.**

**Known follow-ups:**
- Single-category pie charts (e.g. "Student Status" when only one status exists in the data) render as a thin wedge instead of a full circle — cosmetic Recharts quirk, not a data bug (legend/tooltip are correct). Will look better once there's more varied real data.
- No automated tests added (Phase 11 territory).

---

## 2026-07-24 full-app smoke test (post Phase 9)

Ran a read-only click-through of every admin section and public page against the real running app (both servers built + started from real, now-committed code). **16/16 pages passed** — no crashes, no console exceptions tied to any tested page.

**Gap found (not a crash, worth planning for):** The public `/testimonials` page shows 3 hardcoded placeholder testimonials (Priya Sharma, Rahul Patel, Sneha Kulkarni) regardless of the admin Testimonials CRUD state (which was empty during this test). The public page appears to not be wired to the backend testimonials data yet. Same likely applies to `/gallery` and `/blog` public pages, which rendered placeholder/gradient cards consistent with their empty admin-side lists — worth confirming during a Phase 2/6 audit pass whether these are meant to go live yet or are intentional placeholders for a still-empty CMS.

**Minor testing artifact (not an app bug):** one navigation to `/admin/courses` bounced to `/login` mid-session (cookie/session dropped once) — re-login fixed it immediately and courses worked fine after. Worth a second look if it recurs during real usage, but wasn't reproducible.

---

## Phase 7 — Communication (in progress, paused)

- Gmail SMTP: app password set in `backend/.env`. Not yet verified end-to-end with an actual test send.
- WhatsApp Cloud API: app "webiGeeks messaging app" exists in Meta developer console, business portfolio "WebiGeeks" selected. The embedded WhatsApp Quickstart signup repeatedly fails with "Onboarding failure" — Chrome DevTools showed the root cause: `Uncaught (in promise) TypeError: Failed to fetch. Refused to connect because it violates the document's Content Security Policy.` Tried: retry, incognito (unconfirmed whether extensions were actually fully disabled). **Resume by:** confirm a genuinely clean browser profile (extensions off via chrome://extensions, not just Incognito), or try the direct path: business.facebook.com → Business Settings → Accounts → WhatsApp Accounts → Add → "Create a WhatsApp account" (a different code path than the failing embedded Quickstart).
- `backend/.env`: `SMTP_PASS` is set; `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_BUSINESS_ACCOUNT_ID` still empty.

---

## Phase 10 — Performance/SEO (started 2026-08-06, two sessions same day)

**Read `MASTER_TASK_BOARD.md` and `GROWTH_BACKLOG.md` first** — they're the real, actively-maintained backlogs for this phase (per-item status, acceptance criteria, decision log). This section is a summary pointer, not the source of truth.

**Two locations exist as of 2026-08-08 — `webigeeks/` (the working copy, this machine only, NOT git-tracked) and `wg-backend/docs/seo-backlog/` (a git-tracked snapshot, added 2026-08-08 specifically so these travel to other devices via `git pull`).** The `webigeeks/` root is not itself a git repo — only `wg-backend`/`wg-frontend` are — so anything living only at that path is invisible from a fresh checkout elsewhere. If editing these boards, **update the `webigeeks/` copy as usual (it's the live one during a session), then copy the changed file(s) into `wg-backend/docs/seo-backlog/` and commit+push before ending a session** so the next device stays current. If you're starting a session on a different device and only see `wg-backend/docs/seo-backlog/`, that's expected — it may be a few sessions stale relative to whatever's on the original machine's `webigeeks/` root; treat it as "current as of last push," same caveat this whole file already asks you to apply to itself.

**Session 1 — technical SEO (audit score 3.9/10 -> ~8.5-9/10), deployed and verified:**
- Root cause was a real audit against the live site + codebase, not assumptions: zero structured data anywhere, zero Gurugram/location targeting in any title or meta description, a broken OG image reference, a sitemap that lied about `lastModified`, a backend `.select()` projection silently stripping the timestamp fields the sitemap fix needed, and a large client-side Three.js bundle (`@react-three/fiber` + `drei`) loaded eagerly on every page via static imports.
- Fixed: `Organization`/`Course`/`FAQPage`/`BreadcrumbList`/`BlogPosting` JSON-LD, Gurugram-targeted metadata, a `next/og`-generated OG image (no external asset needed), real sitemap timestamps + missing blog entries, noindexed auth utility pages, and `next/dynamic`-based code-splitting of the Three.js hero across all 11 pages that render it.
- **Real, same-day, same-infrastructure Lighthouse before/after on production:** Performance 58 -> 87, TBT 2,310ms -> 300ms (-87%), LCP 3.9s -> 2.7s. This is the one number in this whole phase that's an actual lab measurement, not a self-assessed score.
- Deployed and curl/Lighthouse-verified live, not just build-verified.

**Session 2 — growth phase (topical/local/programmatic SEO), deployed and verified:**
- Explicitly pushed back on part of the brief: it asked for a fleet of neighborhood/fee/duration micro-pages (`/python-course-sector-14`, `/python-course-fees`, etc.) — declined as the scaled-content-abuse/doorway-page pattern Google's spam policies flag, real risk of a site-wide penalty for a single-campus business. Built one genuinely-differentiated location page per real course instead (12 total: MERN, Python, Data Analytics, Power BI, Data Science, AI, SQL, Java, C/C++, MS Excel, React, TypeScript), refactored onto a shared template (`src/data/locationPages.ts` + `src/components/location-pages/CourseLocationPage.tsx`) rather than hand-duplicated files. Full reasoning in `GROWTH_BACKLOG.md`'s decision log (D1).
- Also shipped: Related-Courses cross-linking between course pages, a Footer course-list bug fix (was hardcoded to 6 of the 12 live courses), and non-code deliverables (`CONTENT_CALENDAR.md`, `GBP_CONTENT_PACK.md`, `BACKLINK_OUTREACH_STRATEGY.md`) — all drafts, nothing posted/sent anywhere since there's no GBP/outreach account access.
- **Found and fixed a real fabricated-content issue, not just a style one:** the About page's founding timeline claimed 2019 and named real companies (TCS, Infosys, Wipro) as placement outcomes — confirmed with the site owner as inaccurate (actual founding: 2023; real alumni companies: KPMG, Extramarks, Ferns N Petals, Descriptive AI, Flick AI, Smart Data Enterprises). Corrected throughout rather than just restyled. A follow-up also removed a separate unverifiable "500+ students placed" headcount claim sitewide per the owner's direction, keeping only the three stats they confirmed as accurate (6 years experience, 100+ batches, 10 courses).
- **Found and fixed a real Navbar bug while doing an unrelated copy pass:** the active-link underline animation broke specifically on the Home<->other-page transition because Home lived at `app/page.tsx` outside the `(public)` route group and rendered its own separate `Navbar` instance — every other page shared one persisted instance via `(public)/layout.tsx`. This was a known-but-unactioned smell (previously logged, low priority) until it had a real symptom. Fixed by moving Home into `(public)/page.tsx`; verified with an actual browser (claude-in-chrome), not just structural reasoning.

**Not started as of 2026-08-06:** Accessibility (WCAG) had not been touched at all this phase. (Real fixes started 2026-08-07 — see below. Still not a full audit, just what one PageSpeed report surfaced.)

**Deploy discipline this phase:** both repos auto-deploy to production from `main` on push (Vercel frontend, Render backend) — every change was committed locally first and only pushed after explicit user confirmation, then verified against the live site (curl + one real Lighthouse pass), not assumed from a successful build.

---

## Phase 10 continued — SEO audit v1→v3, first production DB write of this project, real PageSpeed-driven accessibility fixes (2026-08-07, three sub-sessions, same day)

**If resuming from a different device: `git pull` on both repos gets you current code.** Everything below is committed and pushed to `main` in both `wg-backend` and `wg-frontend` as of 2026-08-07 night. The one thing that does **not** travel with git: a published Claude Artifact (`SEO Audit — v3.0`) with the full scorecard/evidence/changelog — it's tied to the Claude account, not the repo. Ask for the link again if you want it; the same information (status, scores, evidence) is captured in prose below and in `webigeeks/MASTER_TASK_BOARD.md`.

### Sub-session A (morning) — status-check audit, v1.0, score 6.9/10

User asked for a fresh SEO audit; scoped down to a status-check against the existing `MASTER_TASK_BOARD.md` rather than redoing prior work blind. Found and fixed **H7**: `/courses/[invalid-slug]` returned HTTP 200 instead of 404. Root-caused to Next.js 16's documented streaming-response behavior (status can't change after a stream starts, so a correctly-thrown `notFound()` still ships 200 with an auto-injected `noindex` tag) — not a real indexation risk per Google's own guidance, but the page was also shipping a **second, conflicting** `index,follow` tag from the root layout's default, which was the actual bug. Fixed in `(public)/courses/[slug]/page.tsx`. C4 (course-specific Gurugram `metaTitle`/`metaDescription`, via `wg-backend/src/scripts/seedCourseSeoMetadata.ts`) applied and verified against **local dev only** — script existed from a prior session but had never been run with `--apply` against production.

### Sub-session B (afternoon/evening) — fresh 31-URL crawl, v2.0, score 7.2/10

Given an explicit "staff-engineer" mission brief to re-audit from scratch (not trust prior notes) and implement/deploy on a `feature/seo-8-audit` branch. A genuine from-scratch crawl of all 31 sitemap URLs (diffing every page's `<head>` against every other page's) found three sitewide defects invisible to prior single-page spot-checks:
- **Every one of the 31 production pages had byte-identical Open Graph/Twitter tags** — `og:title`/`og:description`/`og:url` all matched the homepage regardless of what page was actually shared. Root cause: Next.js metadata merging replaces `openGraph`/`twitter` objects wholesale, not per-field, so any page that didn't set its own inherited the root layout's. Fixed with a new shared helper `wg-frontend/src/lib/seo.ts` (`pageMetadata()`), applied across 21 page/route files plus the homepage's root layout.
- **19 of 31 pages had no self-referencing `<link rel="canonical">`** at all (every static page + all 12 `/courses/[slug]` pages). Fixed by the same `pageMetadata()` helper; homepage's canonical needed a separate one-line hotfix to `layout.tsx` since it has no per-page metadata export.
- **7 of 12 course pages had zero FAQs** in the DB (Power BI, SQL, Java, C/C++, MS Excel, React JS, TypeScript) → no `FAQPage` schema. New script `wg-backend/src/scripts/seedCourseFaqs.ts` (dry-run by default, same pattern as the metadata script) derives FAQs **only from real DB fields** (duration/mode/level/fees) — no invented curriculum/placement claims. Applied to **local dev only** this sub-session (5 of 7 matched — `react-js`/`typescript` were missing from local dev's seed data, a dev-environment gap, not a production one).

Also shipped: direct-answer sentences on the homepage + all 12 course pages (factual "what/who/where" sentences for AI Overviews/LLM browsing, derived from real `siteConfig`/course fields), and footer links to all 12 Gurugram location pages (previously link-starved — one inbound link each from their matching course page, nothing from footer/navbar/homepage). Re-checked the old L3 hydration-error finding via a real browser console check against production — not reproducible.

Merged `feature/seo-8-audit` → `main` in both repos, deployed, re-verified every claim against production post-deploy (not the local build). One real self-caught process mistake: a stale `cd` meant a commit meant for `wg-backend` was initially made in `wg-frontend` with a mismatched message — caught via `git remote -v` before pushing, fixed with `commit --amend`. **Lesson recorded in `webigeeks_env_gotchas` memory: always `pwd && git remote -v` immediately before any commit/merge/push in this two-repo project.**

### Sub-session C (night) — first-ever production database write, v3.0, score 7.8/10

User asked to ship C4 and the FAQ script to production for real — this project had never had production Atlas access in any prior session. What it actually took:
1. No CLI/local credential existed. Checked the browser for an active Render session — not logged in. User logged in themselves.
2. **Render's Web Shell for `wg-backend` is NOT the deployed repo checkout** — no `.git`, no `dist/`, `src/scripts/` had only one unrelated file, despite the Render deploy log showing current commits as live. Don't assume it has the current source tree.
3. User pasted the production `MONGODB_URI` directly in chat instead. Passed inline to local script runs only, never written to any file, redacted in all output shown back.
4. Node's `mongodb+srv://` connection failed at the DNS SRV lookup stage (`querySrv ECONNREFUSED`) in this sandbox specifically, even though plain `nslookup -type=SRV` resolved the same record fine. Worked around: resolve the 3 shard hostnames + the `authSource`/`replicaSet` TXT record via `nslookup`, connect with an equivalent direct `mongodb://` URI instead of the `+srv` form.
5. **The production dry-run itself caught a real bug before writing anything**: the FAQ script's article-agreement logic produced "a intermediate-level course" for React JS/TypeScript (both `intermediate` level). Fixed, re-dry-ran to confirm, then applied.
6. Both scripts confirmed written via the backend's single-course API endpoint (`GET /api/courses/:slug`) immediately after writing (the list endpoint doesn't project `metaTitle`/`faqs` — same pattern as the pre-existing `updatedAt`/`createdAt` projection gap, H1a), then all 12 course pages re-checked live with correct titles/`FAQPage` schema after each page's 60s ISR data-cache window elapsed.
7. **Unrelated but important, found while in the Render dashboard: the account has a failed-payment warning** (RBI card-charging rules block Render's auto-charge) — risks service suspension. Flagged to the user, not something a coding session can fix.

### Sub-session D (later night) — real user-run PageSpeed Insights report, five genuine fixes shipped

User ran an actual Google PageSpeed Insights scan (mobile + desktop) against production and shared both the live report and a desktop PDF export — asked to fix what it found.

**Before any fixing, hit unrelated in-progress work and stopped:** found substantial **uncommitted** changes on a `redesign/premium-ui-2026` branch (Hero/Footer/Navbar/`globals.css`/`layout.tsx` rewritten, `InquiryPopup.tsx` deleted, new `SocialIcons.tsx`), plus a **separate git worktree** at `.claude/worktrees/redesign-premium-ui-2026`. Confirmed with the user this is their own real work before touching anything — see the "Critical context" section at the top of this file for the full detail and recovery instructions (`git stash pop` on that branch).

**Five real findings fixed, all on branch `fix/pagespeed-findings` → merged → deployed → re-verified live:**
1. **`www.webigeeks.com` served the site directly, HTTP 200, zero redirect** to the canonical bare domain — self-canonicalized in metadata but any real visitor/backlink using "www." bypassed the canonical host entirely. Fixed with a permanent host-conditioned redirect in `next.config.ts` (`redirects()` with a `has: [{type: 'host', value: 'www.webigeeks.com'}]` condition). Verified live: `curl -I https://www.webigeeks.com/` → `308` → `https://webigeeks.com/`.
2. **Heading order violated sequential descent** (H1 → H3, skipping H2) — the Hero's embedded "Get Free Career Counseling" lead-capture card used `h3` with nothing at `h2` first. Promoted to `h2` (and its conditional "Thank You!" success state from `h4` to `h3`, keeping the same one-level relationship).
3. **Course `<select>` and name/phone/email inputs had no accessible name** (placeholder text only) across the Hero form, `InquiryPopup`, and `ExitIntentPopup` — added `aria-label` to all of them. This is almost certainly the exact element Google's new experimental **"Agentic browsing"** Lighthouse category flagged independently (checks how well AI browsing agents can parse the page) — worth knowing this category exists now, it's directly relevant to future AI-search/GEO work on this site.
4. **Icon-only close buttons had no accessible name** — `InquiryPopup`, `ExitIntentPopup`, and the mobile nav drawer's close button, all just an `<X>` icon with no `aria-label`.
5. **Sitewide WCAG-AA contrast failure**: `--color-text-muted` (`#94A3B8`, used in 51 files as the shared "secondary/muted text" design token) computes to ~2.66:1 against the site's white/near-white backgrounds — well under AA's 4.5:1 minimum for normal text. Fixed once at the token level (`globals.css`) to `#64748B` (~4.76:1, same hue family, passes AA).

**Investigated, not fixed — "Browser errors were logged to console"** (flagged under Best Practices): checked via a live browser console on both `webigeeks.com` and `www.webigeeks.com`, found zero errors on either in a normal session, same conclusion as the earlier L3 hydration-error check. Left open/monitor rather than fabricating a fix for something that doesn't reproduce.

**Two real infrastructure bugs found and fixed while re-running QA, unrelated to PageSpeed itself:**
- **A stale `.next` build cache served genuinely wrong output** — built HTML had CSS classes that don't exist anywhere in current source. Root cause not fully pinned down (suspected: `.next/cache` not invalidating correctly across the branch-switch away from `redesign/premium-ui-2026`, since `.next` isn't git-tracked and branch switches don't touch it). Fixed with `rm -rf .next` + clean rebuild. **If built/served content ever doesn't match the source files again, don't assume you're misreading the source — wipe `.next` first.**
- **`vitest.config.ts`'s `exclude` list was a real bug**: `["node_modules", ".next", "tests-e2e"]` are literal string matches, not globs, so nested `node_modules` (inside the sibling worktree above) were never actually excluded — `vitest run` silently scanned 200+ extra files including a dependency's own test suite, then a copy of this project's own WIP test files from the redesign worktree, producing 69 false failures. Fixed to `["**/node_modules/**", ".next", "tests-e2e", ".claude/**"]` — back to the real baseline of 5 files / 26 tests, all passing. **If `npm test` output ever looks wildly larger than ~5 files again, suspect this regressing, not the app.**

**One pre-existing, unrelated lint error noticed but not fixed (out of scope this session):** `wg-frontend/src/components/layout/Navbar.tsx:39` — `setIsMobileMenuOpen(false)` called synchronously inside a `useEffect`, flagged by the `react-hooks/set-state-in-effect` rule as an ESLint **error** (not warning). Confirmed pre-existing on `main`, not touched by any of tonight's edits. `npx eslint` on this file will fail until it's fixed.

**Score progression this whole day: 3.9 (2026-08-06 baseline) → 6.9 (v1.0) → 7.2 (v2.0) → 7.8 (v3.0).** Gap to the 8.0 target is now concentrated almost entirely in **Content & Authority** (still ~4.5/10) — no blog posts, no placements/success-stories page, no faculty pages. That's real content-authoring work measured in days, not an engineering fix. Every other scorecard category is 7.0+.

**Immediate next steps if resuming:**
1. **Content & Authority is the highest-leverage remaining item** for the 8.0 target — building the Placements/Success Stories page (H6 on `MASTER_TASK_BOARD.md`) is the single biggest lever; real testimonial data already exists in the DB, this is content assembly, not new data collection.
2. **Resolve the Render payment failure** — not SEO, but higher priority than anything else here; an unpaid invoice risks taking the whole backend down.
3. **Fix the pre-existing Navbar lint error** noted above if it's blocking anything.
4. **Run a fresh production Lighthouse pass** — confirm the PageSpeed-driven fixes (all database/metadata/CSS-token only) really left Performance/Accessibility scores where expected, rather than assuming.
5. **Decide on H7's HTTP status** — accept the current noindex-mitigated 200 (real evidence it's not an indexation risk), or approve building a `proxy.ts` existence check for a true 404 (adds a backend round-trip to every course-page view — a real perf trade-off, not free).
6. **C6 decision needed**: Graphic Designing / UI-UX courses have no product behind them — business call on whether to build them or drop the keywords.
